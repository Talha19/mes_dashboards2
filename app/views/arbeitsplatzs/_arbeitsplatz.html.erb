<li class="list-group-item active">
	<h4 class="list-group-item-heading"> <%= arbeitsplatz.name %> </h4>
	<p class="list-group-item-text">
		<%= arbeitsplatz.bezeichnung %>
	</p>
	<p>
		<%= link_to 'löschen', [arbeitsplatz.werk, arbeitsplatz],
		class: 'btn btn-danger',
		method: :delete,
		data: { confirm: 'Sind Sie sicher?' } %>
		
		<td> <%= link_to "Details zu Arbeitsplatz", werk_arbeitsplatzs_path(arbeitsplatz), class: 'btn btn-success' %>
	</p>
</li>