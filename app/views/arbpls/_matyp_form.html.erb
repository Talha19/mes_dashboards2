
<div id="wrapper">
	<div id="page-wrapper">
		<div class="row">
			<div class="col-lg-12">
<%= bootstrap_form_for ([@arbpl, @matyp]) do |f| %>
  <% if @matyp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@matyp.errors.count, "error") %> prohibited this statistic from                 
        being saved:</h2>

      <ul>
        <% @matyp.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.text_field :name %><br />
    <%= f.text_area :beschr %>
    <div class="actions">
      <%= f.submit %>
    </div>
  </div>
<% end %>
			</div>
		</div>
	</div>
</div>